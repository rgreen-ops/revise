{
  "name": "skyline-theme",
  "version": "1.0.0",
  "description": "Skyline — Football Club HTML template (sky blue & gold) built with Tailwind CSS",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/css/main.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/css/main.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
