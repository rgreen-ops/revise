{
  "name": "sideline-theme",
  "version": "1.0.0",
  "description": "Sideline — Grassroots Junior Football Club HTML template built with Tailwind CSS",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/css/main.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/input.css -o assets/css/main.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
